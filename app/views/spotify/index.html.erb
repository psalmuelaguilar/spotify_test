TEST
<%= form_tag(spotify_index_path, method: :get) do %>
  <%= text_field_tag 'spotify_search' %>

  <%= submit_tag('search')%>
<% end %>

<% @response.each do |resp| %>
  <div>
    <b> <%= resp[:name] %> </b>
    <%= resp[:external_urls]%>
  </div>
<% end %>
